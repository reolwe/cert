plugin: aws_ec2
aws_profile: default
regions:
  - us-east-1

hostnames:
  - ip-address
  -
#keyed_groups:
#  - prefix: area
#  - key: Name

groups:
  build: "'build' in tags.Name"
  stage: "'stage' in tags.Name"